/**
 * 基础使用示例
 *
 * 演示：
 * 1. 如何创建和使用 Agent
 * 2. 简单的新闻查询
 * 3. 不同的查询方式
 */
import { createNewsAgent } from '../src/agent.js';
async function example1_basicQuery() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 1: 基础新闻查询                                              ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt('展示过去一周的最新 AI 新闻');
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example2_chineseNews() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 2: 中文 AI 新闻                                              ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt('获取最新的中文AI新闻，重点关注大模型发展');
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example3_specificCategory() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 3: 特定类别（NLP）                                           ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt('获取过去3天的自然语言处理新闻和趋势');
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example4_recentBreakthrough() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 4: 最近的技术突破                                            ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt('昨天有哪些重大的 AI 突破？给我一个简要的总结。');
    console.log('\n' + '═'.repeat(70) + '\n');
}
/**
 * 运行所有示例
 */
async function runAllExamples() {
    console.log('\n🚀 运行基础使用示例\n');
    console.log('这些示例演示了 AI 新闻 Agent 的核心功能。\n');
    console.log('随时按 Ctrl+C 停止。\n');
    console.log('═'.repeat(70) + '\n');
    try {
        // 运行示例 1
        await example1_basicQuery();
        // 可选：运行其他示例（注释掉以节省 API 调用）
        // await example2_chineseNews();
        // await example3_specificCategory();
        // await example4_recentBreakthrough();
        console.log('\n✅ 所有示例成功完成！\n');
    }
    catch (error) {
        console.error('\n❌ 示例失败:', error);
        process.exit(1);
    }
}
// 运行示例
runAllExamples().catch(console.error);
//# sourceMappingURL=basic-usage.js.map