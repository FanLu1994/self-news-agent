/**
 * 高级使用示例
 *
 * 演示：
 * 1. 多轮对话
 * 2. 自定义参数
 * 3. 对比分析
 * 4. 特定主题深挖
 */
import { createNewsAgent } from '../src/agent.js';
async function example1_multiTurn() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 1: 多轮对话                                                  ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    // 第一轮：获取新闻
    console.log('👤 用户: 获取过去一周的 AI 新闻\n');
    await agent.prompt('获取过去一周的 AI 新闻');
    console.log('\n' + '─'.repeat(70) + '\n');
    // 第二轮：深入分析
    console.log('👤 用户: 这些文章中哪篇最重要？为什么？\n');
    await agent.prompt('这些文章中哪篇最重要？为什么？');
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example2_comparison() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 2: 对比分析                                                  ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt(`对比最新的 AI 发展：
1. 英文来源（HackerNews）
2. 中文来源（中文科技媒体）

突出西方和中国 AI 社区在关注点和优先事项上的差异。`);
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example3_specificTopic() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 3: 特定主题深度分析                                          ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt(`找出过去一周所有与 GPT、Claude 或 Gemini 相关的新闻。
分析：
1. 宣布了哪些新功能或能力？
2. 这些模型是如何被比较的？
3. 行业反应如何？`);
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example4_trendAnalysis() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 4: 趋势分析                                                  ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt(`获取过去7天的 AI 新闻并识别：
1. 之前不突出的新兴趋势
2. 多篇文章中反复出现的主题
3. 基于当前发展的潜在未来方向

重点关注机器学习和自然语言处理类别。`);
    console.log('\n' + '═'.repeat(70) + '\n');
}
async function example5_briefSummary() {
    console.log('╔════════════════════════════════════════════════════════════════════╗');
    console.log('║  示例 5: 简要总结风格                                              ║');
    console.log('╚════════════════════════════════════════════════════════════════════╝\n');
    const agent = createNewsAgent();
    await agent.prompt('给我一个今天 AI 新闻的快速简要总结 - 只要关键标题的要点形式。');
    console.log('\n' + '═'.repeat(70) + '\n');
}
/**
 * 运行所有高级示例
 */
async function runAdvancedExamples() {
    console.log('\n🚀 运行高级使用示例\n');
    console.log('这些示例演示了高级功能和用例。\n');
    console.log('随时按 Ctrl+C 停止。\n');
    console.log('═'.repeat(70) + '\n');
    try {
        // 选择要运行的示例（注释掉不需要的以节省 API 调用）
        // await example1_multiTurn();
        await example2_comparison();
        // await example3_specificTopic();
        // await example4_trendAnalysis();
        // await example5_briefSummary();
        console.log('\n✅ 高级示例成功完成！\n');
    }
    catch (error) {
        console.error('\n❌ 示例失败:', error);
        process.exit(1);
    }
}
// 运行示例
runAdvancedExamples().catch(console.error);
//# sourceMappingURL=advanced-usage.js.map